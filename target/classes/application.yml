spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommjava?createDatabaseIfNotExist=true
    username: root
    password: Manju@8055
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: embedded